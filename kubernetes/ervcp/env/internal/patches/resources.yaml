---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ervcp
  labels:
    component: ervcp
spec:
  template:
    spec:
      containers:
        - name: ervcp
          resources:
              limits:
                cpu: "0.1"
                memory: 50Mi
